#!/bin/bash

cd <%= @options["data_dir"] %>

exec 2>&1
exec chpst -u <%= @options["user"] %> -o <%= @options["server"]["max_open_files"] %> /usr/local/bin/redis-server <%= @options["conf_dir"] %>/redis.conf
